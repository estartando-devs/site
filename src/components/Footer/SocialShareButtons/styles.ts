import styled from 'styled-components';
import { Card } from '../../Card';

export const SocialShareContainer = styled.div`
  display: flex;
  gap: 16px;
  justify-content: center;
  align-items: center;
`;

export const SocialShareContent = styled(Card)`
  padding: 8px;
  justify-content: center;
  align-items: center;
`;

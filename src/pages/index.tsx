import React from 'react';
import { Layout, Footer, Header, OurResults } from '../components';

const Home = () => (
  <Layout>
    <Header />
    <OurResults />
    <Footer />
  </Layout>
);

export default Home;
